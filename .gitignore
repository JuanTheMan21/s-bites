# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Env & secrets
.env
.env.local
*.key

# Pipeline output
artifacts/
*.mp4
*.wav
*.mp3

# LangGraph checkpoints
checkpoints/
*.sqlite
*.sqlite3

# Node / frontend
node_modules/
web/dist/
web/.vite/

# HyperFrames
.hyperframes/
out/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local overrides
.claude/settings.local.json

# Impeccable: config.json is shared (committed); consent + the dedup cache are machine-local,
# including the skill bundle's own internal copy under .claude/skills/impeccable/scripts/
**/.impeccable/config.local.json
**/.impeccable/hook.cache.json

# RepoWise index -- machine-local caches, vector store, and session databases.
# Regenerated by `repowise init`; never shared between machines.
.repowise/
